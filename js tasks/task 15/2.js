let sentence = "I am learning js";
let subWord = "js";

if (sentence.includes(subWord)) {
  console.log(`'${subWord}' exists in the string.`);
} else {
  console.log(`'${subWord}' does NOT exist in the string.`);
}
